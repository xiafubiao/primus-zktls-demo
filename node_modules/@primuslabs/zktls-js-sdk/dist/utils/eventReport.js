import { reportEvent } from '../api';
async function eventReport(rawDataObj) {
    try {
        await reportEvent(rawDataObj);
    }
    catch (error) {
        console.error('event report failed:', error);
    }
}
export { eventReport };
//# sourceMappingURL=eventReport.js.map