import { request } from '../utils/httpRequest';
import { BASEAPI } from '../config/constants';
export function getAppQuote(params) {
    return request({
        url: `${BASEAPI}/public/app/quote`,
        method: 'GET',
        params
    });
}
export function reportEvent(rawDataObj) {
    const data = {
        eventType: 'ATTESTATION_GENERATE',
        rawData: JSON.stringify(rawDataObj)
    };
    return request({
        url: `${BASEAPI}/public/event/report`,
        method: 'POST',
        data
    });
}
//# sourceMappingURL=index.js.map